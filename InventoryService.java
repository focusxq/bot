package tgbot.service;

import tgbot.model.InventoryItem;
import tgbot.model.Character;

public class InventoryService {
    public void addItem(Character character, InventoryItem item) {
        // Implement the logic to add item to character's inventory
    }

    public void removeItem(Character character, InventoryItem item) {
        // Implement the logic to remove item from character's inventory
    }

    public void transferItem(Character fromCharacter, Character toCharacter, InventoryItem item) {
        // Implement the logic to transfer item between characters
    }
}